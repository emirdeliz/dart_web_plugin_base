runs:
  using: 'composite'
  steps:
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
    - run: flutter clean && flutter pub get
      shell: sh
    - run: cd example/driver_test && rm -rf chromedriver
      shell: sh
    - uses: nanasess/setup-chromedriver@v1
