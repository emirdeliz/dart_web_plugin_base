name: Test
on: [push]
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions
      - run: cd example && sh scripts/run-integration-test.sh
